/* complete the source code file for derived class SavingsAccount
* You need to:
 * Define the parameterized constructor
 * Define the member function: applyInterest() by the following steps:
 * 1. calculate interest: balance * interestRate <= the new data member in this class
 * 2. add the interest to balance
 * Make sure negative deposits, negative/zero withdrawals, or overdrafts are not allowed; Handle all invalid cases with std::cout messages
 */

#include "SavingsAccount.h"
